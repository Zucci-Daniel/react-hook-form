import {React, TouchableOpacity} from '../../imports/all_RnComponents';
import Cor from '../../assets/svg-raw/correct.svg';

const Correct = ({onPress}) => {
  return <Cor width={100} height={100} />;
};

export default Correct;
