import React from 'react';
import Fly from '../../assets/svg-raw/transfer2.svg';

const TransferIcon = ({width = 40, height = 40}) => {
  return <Fly width={width} height={height} />;
};

export default TransferIcon;
